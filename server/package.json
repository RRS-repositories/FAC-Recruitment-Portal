{
  "name": "fac-recruitment-server",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "FAC recruitment portal API — applications, review, interview booking.",
  "scripts": {
    "dev": "node --watch index.js",
    "start": "node index.js",
    "test": "node --test \"**/*.test.js\"",
    "migrate": "node scripts/apply-migrations.js",
    "migrate:dry": "node scripts/apply-migrations.js --dry-run"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^5.1.0",
    "express-rate-limit": "^7.5.1",
    "pg": "^8.13.1"
  }
}
